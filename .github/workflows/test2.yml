name: test-worker-1

on: [push]

jobs:
  build:

    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v3
      # You can test your matrix by printing the current Python version
      - name: Display Python version
        run: python3 -c "import sys; print(sys.version)"
      # - name: Hello World
      #   run: python -c "print('Hello worldcito')"
